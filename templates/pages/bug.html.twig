
        <div class="row justify-content-center">
            <div class="col-xl-6">
                <h2 class="mb-4">Rapportez un problème survenu sur le site</h2>
                {{ form_start(problemForm) }}
                {{ form_end(problemForm) }}
            </div>
        </div>
